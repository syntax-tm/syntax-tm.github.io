export default interface StatThemeAudioConfig {
  cursor: string;
  ok: string;
  cancel: string;
  notification: string;
  error: string;
  enable: string;
  disable: string;
  open: string;
};
